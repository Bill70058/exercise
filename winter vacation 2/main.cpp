#include <iostream>
#include <cstdio>
#include <cstdlib>

//输入一个字符，如果是字母，输出其对应的ASCII码，如果是数字，按原样输入，否则给出提示信息“输入错误！”。

using namespace std;

int main()
{
    char a;
    printf("请输入字符:");
    while(a != EOF)
    {
        if('a' <= a && a <= 'z')||('A' <= a && a <= 'Z')
        {

        }
    }


    return 0;
}
